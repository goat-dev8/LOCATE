# Names only. Real values live in .env (gitignored) and in the Render / Vercel / GitHub secret stores.

# deploy + repo access (operator machine and CI only)
RENDER_API_KEY=
VERCEL_TOKEN=
GITHUB_TOKEN=
GITHUB_REPO=
RENDER_OWNER_ID=
VERCEL_TEAM_ID=

# database (backend only; never shipped to the browser)
DATABASE_URL=
DIRECT_URL=

# solana
SOLANA_RPC_URL_DEVNET=
SOLANA_RPC_URL_MAINNET=
LOCATE_PROGRAM_ID=
LOCATE_DEPLOYER_KEYPAIR=

# market data (advisory only)
JUPITER_API_BASE=
JUPITER_API_KEY=
PRESTOCKS_API_BASE=

# backend runtime
SOLANA_CLUSTER=
SOLANA_RPC_URL=
CORS_ALLOWED_ORIGINS=
PORT=

# frontend build (public values only)
VITE_API_BASE_URL=
VITE_SOLANA_CLUSTER=
VITE_SOLANA_RPC_URL=
VITE_LOCATE_PROGRAM_ID=
